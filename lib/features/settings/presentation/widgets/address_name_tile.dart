import 'package:flutter/material.dart';

class AddressNameTile extends StatelessWidget {
  const AddressNameTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Column(
          children: [],
        )
      ],
    );
  }
}
